// Kept identical to `DASHBOARD_BOOTSTRAP_MARKER` in
// `packages/specdev-dashboard/src/index.ts`. Duplicated (one short string) so
// the packaging tool does not take a dependency on a product package.
export const DASHBOARD_BOOTSTRAP_MARKER = 'Nevo SpecDev dashboard command is available.';
